const theme = {};
theme.colors = {
  black: "#000000",
  white: "#FFFFFF",
};

theme.sizes = {
  container: `${800 / 16}em`,

  space: `${10 / 16}em`,

  font1: `${20 / 16}em`,
  fontTitle: `${60 / 16}em`,
  fontLink: `${30 / 16}em`,
  fontTitleBottom: `${40 / 16}em`,

  paddingText: `${10 / 16}em`,
};

theme.fonts = {
  paragraph: "athiti",
  title: "nobleman",
};

export default theme;
